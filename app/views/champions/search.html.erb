<p class="mb-5">
    Found <%= pluralize(@champions.count, 'Champion') %> When searching for: <%= params[:keywords] %>
</p>

<ul>
    <% @champions.each do |champ| %>
        <li><%= link_to champ.name, champ%> </li>
    <% end %>
</ul>